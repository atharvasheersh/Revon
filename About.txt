Here's a message you can send as-is:

---

**Hey — here's exactly what we're building tonight and why. Read this fully before starting, it'll save us from vibe-coding the wrong thing.**

**The idea in one line:** a Git-like versioned database, but instead of copying data per version, we use a hash-based tree where unchanged data is automatically shared between versions — not duplicated. That sharing is the entire point of the project, so tonight has to prove it actually works, live.

**The one flow we need working by tomorrow, nothing else:**
```
commit(v1) → change a few keys → commit(v2) → diff(v1, v2) → print stats
```

**Build it in this order, don't skip ahead:**

1. **In-memory tree, not disk-backed.** Python is fine, keep it simple — dict-based nodes. Every node's ID = hash of its own content (e.g. `hashlib.sha256(str(sorted(children)).encode())`). Fixed branching factor (like 4 or 8 children per internal node) is fine, don't overthink the split logic tonight.

2. **`commit()`** — takes the current key-value state, builds/updates the tree, returns the new root hash. Store `{version_number: root_hash}` somewhere simple.

3. **`diff(root_hash_1, root_hash_2)`** — walk both trees in parallel starting from their roots. **If two nodes being compared have the same hash, stop — don't recurse into them, they're identical.** Only recurse into subtrees where the hashes differ. Return the actual list of changed keys at the end. This "stop early on matching hash" line is the entire idea — if you get this right, everything else follows.

4. **Reuse stats** — while committing v2, count how many nodes were newly created vs. how many already existed (same hash as something in v1). Print something like `commit v2: 3 new nodes, 214 reused (98.6% shared)`. This is the number that makes the demo land — it's visual proof the sharing is real, not just a diff running fast.

**Don't build tonight:** disk storage, WAL, buffer pool, checkout/rollback, branching, merge. All real, all planned for the next 3 months, none of it matters for tomorrow. If steps 1–4 are solid, we have a complete demo. If we half-build five things instead, we have nothing to show.

**Why this specific thing proves the point:** the whole pitch tomorrow is "diffing and versioning doesn't need to scan or copy the whole dataset — only the parts that actually changed." Step 3's early-stop-on-matching-hash is literally that claim in code. Step 4's percentage is that claim as a number on screen. That's it — that's the demo.

If you get through this early, next priority (optional) is `log()` to print version history, then `checkout(root_hash)` to rebuild state at any old version. But only after 1–4 are actually working end to end.